# Practical Submission

This repository contains two React applications developed using Vite for practical assignments.

## Projects

### PracticalEndSem2
A React app that fetches user data from an API (JSONPlaceholder) and displays it.

### PracticalEndSemPractical
A React app demonstrating parent-child component communication with props.

Each project is in its own directory with its own dependencies and build setup.